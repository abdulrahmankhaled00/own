import type React from "react"
import type { Metadata } from "next"
import { Inter, Playfair_Display } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"], variable: "--font-inter" })
const playfair = Playfair_Display({ subsets: ["latin"], variable: "--font-playfair" })

export const metadata: Metadata = {
  title: "OWN Developments - Excellence in Real Estate",
  description:
    "OWN Developments stands as a beacon of progress in the Egyptian real estate market, creating sustainable environments where businesses and families can thrive.",
  keywords: "real estate, Egypt, construction, development, Al Qima, OWN",

}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className={`${inter.variable} ${playfair.variable}`}>
      <body className={inter.className}>{children}</body>
    </html>
  )
}
